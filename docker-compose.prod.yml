name: wg-prod

services:
  wg:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./instance:/app/instance    # generierte PDFs
    env_file:
      - .env.prod
    # DATABASE_URL muss in .env.prod gesetzt sein, z.B.:
    # mysql+pymysql://wg:passwort@192.168.0.12:3307/wgbuchhaltung_prod?charset=utf8mb4
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/auth/login"]
      interval: 30s
      timeout: 10s
      retries: 3
